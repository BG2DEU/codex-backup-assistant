# 阶段 A 验收记录

## 基本信息

| 项目 | 内容 |
| --- | --- |
| 阶段 | A：发现与风险验证 |
| 状态 | 已通过 |
| 开始日期 | 2026-06-17 |
| 完成日期 | 2026-06-17 |

## 验收项

| 编号 | 验收项 | 状态 | 证据 |
| --- | --- | --- | --- |
| A-01 | 识别 Codex 数据根目录 | 通过 | 可识别顶层项、文件数量和体量 |
| A-02 | 对 Codex 顶层项进行备份政策分类 | 通过 | 可区分包含、双轨、凭据排除、临时排除、清单和待审查项 |
| A-03 | 识别 Git 项目和常见非 Git 项目 | 通过 | 可识别多个 Git 项目和标记文件项目 |
| A-04 | 只读获取 Git 分支、变更和远程摘要 | 通过 | 可识别存在变更的仓库和远程状态 |
| A-05 | 不跟随目录联接和符号链接 | 通过 | 可识别并跳过重解析点，无循环遍历 |
| A-06 | 识别同物理磁盘上的多盘符 | 通过 | 可识别同物理磁盘上的不同盘符 |
| A-07 | 报告不包含凭据或文件内容 | 通过 | 烟雾测试的认证和 `.env` 哨兵值均未出现在报告中 |
| A-08 | 烟雾测试通过 | 通过 | 1 个 Git 项目、4 个 Codex 项、25 个源文件，0 警告 |
| A-09 | 真实机器扫描成功 | 通过 | 可扫描较大项目集合，报告不进入 Git |
| A-10 | 扫描前后 Git 状态未被探针修改 | 通过 | 仓库索引哈希和 `git status` 指纹保持不变 |
| A-11 | 验证 Codex 记录对项目的实际覆盖率 | 通过 | Codex `cwd` 能覆盖多数相关项目，但不是完整注册表 |
| A-12 | 证明轻量标志补漏的必要性 | 通过 | 存在未出现在 Codex 会话路径中的项目，需要补漏 |

## 执行记录

### 2026-06-17 烟雾测试

- 使用临时目录创建 Git 项目、Codex 会话、缓存、配置和认证文件。
- 探针正确将 `sessions` 分类为双轨导出，将 `auth.json` 分类为凭据排除，将 `cache` 分类为临时排除。
- 报告未包含测试令牌或 `.env` 内容。
- 扫描前后 25 个源文件的 SHA-256 完全一致。

### 2026-06-17 真实机器扫描

- 扫描可在真实使用环境中完成，并生成项目体量和 Codex 数据分类摘要。
- 发现尚未建立稳定策略的 Codex 顶层项时，按 `UnknownReviewRequired` 安全处理，没有静默排除。
- 扫描报告保存在系统临时目录，因包含私有路径而未加入 Git。

### 2026-06-17 Codex 路径覆盖验证

- 仅读取会话前部元数据中的 `cwd`，未读取对话正文或项目源码。
- Codex 会话路径可直接归属到多数现存项目。
- 仍存在完全未被会话路径引用的项目，因此必须保留项目标志补漏。
- 发现策略正式更正为“Codex 记录为主、项目标志补漏、用户最终确认”，不执行文件内容级整盘扫描。

## 阶段结论

阶段 A 全部验收项通过。发现规则可进入正式强类型实现，其顺序必须是 Codex 记录优先、轻量项目标志补漏、用户确认。同时，3 个 `UnknownReviewRequired` 项在真正导出前仍必须保持用户可见和手动确认。
